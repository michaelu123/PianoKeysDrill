An installation of music21j (npm install music21j) failed during npm run build, 
resp. when node build was called, apparently because
music21j has so many other dependencies. As I use only chords, I stripped music21j down.
